"use client";

import Image from "next/image";
import { Link } from "@/i18n/navigation";
import { ArrowLeft, ArrowRight, Check, Compass, MessageCircle, RotateCcw } from "lucide-react";
import { useMemo, useState } from "react";
import { useTranslations } from "next-intl";
import { Button } from "@/components/ui/button";
import { Turnstile } from "@/components/turnstile";
import { requestQuote } from "@/lib/quote";
import { discoveryImage, discoveryDestinations, scoreDestinations, type DiscoveryAnswers, type DiscoveryDestinationProfile } from "@/lib/discovery-data";
import type { Package } from "@/lib/packages";

type PublishedDestination = { id: string; slug: string; nombre: string };
const optionKeys = {
  travelerType: ["pareja", "familia", "amigos", "solo"],
  experiences: ["playa", "ciudad", "aventura", "lujo", "familiar", "fiesta", "romance"],
  budget: ["economico", "medio", "alto"],
  climate: ["calido", "templado", "indiferente"],
  duration: ["corta", "media", "larga"],
  interests: ["gastronomia", "cultura", "naturaleza", "compras", "playa", "vida nocturna", "familia"],
} as const;
const initialAnswers: DiscoveryAnswers = { travelerType: "pareja", experiences: [], budget: "medio", climate: "indiferente", duration: "media", interests: [], travelers: 1 };

function slugForDestination(value: string) { return value.normalize("NFD").replace(/[\u0300-\u036f]/g, "").toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-|-$/g, ""); }

export function DiscoveryAssistant({ destinations, packages }: { destinations: PublishedDestination[]; packages: Package[] }) {
  const t = useTranslations("discover");
  const [answers, setAnswers] = useState<DiscoveryAnswers>(initialAnswers);
  const [step, setStep] = useState(0);
  const [completedAnswers, setCompletedAnswers] = useState<DiscoveryAnswers | null>(null);
  const [turnstileToken, setTurnstileToken] = useState("");
  const [busy, setBusy] = useState(false);
  const [error, setError] = useState(false);
  const profiles = useMemo(() => destinations.map(destination => discoveryDestinations.find(profile => profile.slug === destination.slug)).filter((profile): profile is DiscoveryDestinationProfile => Boolean(profile)), [destinations]);
  const recommendations = useMemo(() => { if (!completedAnswers) return []; return scoreDestinations(completedAnswers, profiles).map(recommendation => ({ ...recommendation, destinationId: destinations.find(destination => destination.slug === recommendation.slug)?.id, displayName: destinations.find(destination => destination.slug === recommendation.slug)?.nombre || recommendation.nombre })); }, [completedAnswers, destinations, profiles]);
  const currentStep = ["travelerType", "experiences", "budget", "climate", "duration", "interests", "travelers"][step];
  const stepTitle = t(currentStep === "budget" ? "budgetQuestion" : currentStep);
  const options = (group: keyof typeof optionKeys) => optionKeys[group].map(key => [key, t("labels." + key)] as [string, string]);
  const isComplete = currentStep === "travelers" ? answers.travelers >= 1 && answers.travelers <= 20 : currentStep === "experiences" ? answers.experiences.length > 0 : currentStep === "interests" ? answers.interests.length > 0 : true;
  function updateAnswer(field: keyof DiscoveryAnswers, value: string | string[] | number) { setAnswers(current => ({ ...current, [field]: value } as DiscoveryAnswers)); }
  function toggleMulti(field: "experiences" | "interests", value: string) { const current = answers[field] as string[]; updateAnswer(field, current.includes(value) ? current.filter(item => item !== value) : [...current, value]); }
  function advance() { if (!isComplete) return; if (step === 6) { setCompletedAnswers(answers); return; } setStep(current => current + 1); }
  function startOver() { setAnswers(initialAnswers); setStep(0); setCompletedAnswers(null); setTurnstileToken(""); setError(false); }
  async function sendToWhatsApp() {
    if (!completedAnswers || !turnstileToken || busy) return;
    setBusy(true); setError(false);
    const pickNames = recommendations.map(item => item.displayName).join(", ");
    const payload = { service: "Descubrimiento", servicio: "descubrimiento", turnstileToken, fields: { Destino: pickNames, "Con quién viaja": t("labels." + completedAnswers.travelerType), Experiencia: completedAnswers.experiences.map(item => t("labels." + item)).join(", "), Presupuesto: t("labels." + completedAnswers.budget), Clima: t("labels." + completedAnswers.climate), Duración: t("labels." + completedAnswers.duration), Intereses: completedAnswers.interests.map(item => t("labels." + item)).join(", "), Viajeros: String(completedAnswers.travelers), Recomendaciones: pickNames }, formData: { travelerType: completedAnswers.travelerType, experiences: completedAnswers.experiences.join(", "), budget: completedAnswers.budget, climate: completedAnswers.climate, duration: completedAnswers.duration, interests: completedAnswers.interests.join(", "), travelers: String(completedAnswers.travelers), recommendations: pickNames } };
    try { await requestQuote(payload); } catch { setError(true); setBusy(false); }
  }
  if (completedAnswers) return <DiscoveryResults recommendations={recommendations} answers={completedAnswers} packages={packages} turnstileToken={turnstileToken} setTurnstileToken={setTurnstileToken} busy={busy} error={error} onSend={sendToWhatsApp} onStartOver={startOver} />;
  return <section className="rounded-panel border border-line bg-canvas p-4 shadow-sm sm:p-8" aria-labelledby="discovery-form-title"><div className="mb-8 flex flex-wrap items-start justify-between gap-4"><div><p className="t-small mb-2 text-brand">{t("step", { current: step + 1, total: 7 })}</p><h2 id="discovery-form-title" className="t-h2">{stepTitle}</h2></div><Compass size={32} strokeWidth={1.75} className="text-brand" aria-hidden="true" /></div><div className="mb-8" role="progressbar" aria-label={t("progress", { current: step + 1, total: 7 })} aria-valuemin={1} aria-valuemax={7} aria-valuenow={step + 1}><div className="h-2 overflow-hidden rounded-btn bg-surface"><div className="h-full bg-brand transition-[width] duration-(--duration-fast) ease-out motion-reduce:transition-none" style={{ width: ((step + 1) / 7) * 100 + "%" }} /></div></div><fieldset className="space-y-4"><legend className="sr-only">{stepTitle}</legend>{currentStep === "travelerType" && <OptionList name="travelerType" options={options("travelerType")} selected={answers.travelerType} onSingle={value => updateAnswer("travelerType", value)} />}{currentStep === "experiences" && <OptionList name="experiences" options={options("experiences")} selected={answers.experiences} onMulti={value => toggleMulti("experiences", value)} multi />}{currentStep === "budget" && <OptionList name="budget" options={options("budget")} selected={answers.budget} onSingle={value => updateAnswer("budget", value)} />}{currentStep === "climate" && <OptionList name="climate" options={options("climate")} selected={answers.climate} onSingle={value => updateAnswer("climate", value)} />}{currentStep === "duration" && <OptionList name="duration" options={options("duration")} selected={answers.duration} onSingle={value => updateAnswer("duration", value)} />}{currentStep === "interests" && <OptionList name="interests" options={options("interests")} selected={answers.interests} onMulti={value => toggleMulti("interests", value)} multi />}{currentStep === "travelers" && <div className="max-w-sm space-y-2"><label className="t-small block" htmlFor="discovery-travelers">{t("travelerNumber")}</label><input id="discovery-travelers" type="number" inputMode="numeric" min={1} max={20} value={answers.travelers} onChange={event => updateAnswer("travelers", Math.min(20, Math.max(0, Number(event.target.value))))} className="t-body h-12 w-full rounded-btn border border-line bg-canvas px-3 py-2 focus-visible:border-brand" aria-describedby="discovery-travelers-help" /><p id="discovery-travelers-help" className="t-small text-ink-soft">{t("travelerHelp")}</p></div>}</fieldset><div className="mt-8 flex flex-wrap items-center justify-between gap-4 border-t border-line pt-6"><Button type="button" variant="ghost" disabled={step === 0} onClick={() => setStep(current => current - 1)}><ArrowLeft size={18} strokeWidth={1.75} aria-hidden="true" />{t("back")}</Button><Button type="button" disabled={!isComplete} onClick={advance}>{step === 6 ? t("recommendations") : t("continue")}<ArrowRight size={18} strokeWidth={1.75} aria-hidden="true" /></Button></div></section>;
}

function OptionList({ name, options, selected, onSingle, onMulti, multi = false }: { name: string; options: [string, string][]; selected: string | string[]; onSingle?: (value: string) => void; onMulti?: (value: string) => void; multi?: boolean }) { return <div className="grid gap-3 sm:grid-cols-2">{options.map(([value, label]) => { const active = Array.isArray(selected) ? selected.includes(value) : selected === value; return <label key={value} className={"flex min-h-12 cursor-pointer items-center gap-3 rounded-btn border px-4 py-3 transition-colors duration-(--duration-fast) ease-out " + (active ? "border-brand bg-brand-tint text-brand-deep" : "border-line bg-canvas hover:bg-surface")}><input type={multi ? "checkbox" : "radio"} name={name} value={value} checked={active} onChange={() => multi ? onMulti?.(value) : onSingle?.(value)} className="h-4 w-4 accent-brand" /><span className="t-body">{label}</span>{active && <Check size={18} strokeWidth={2} className="ml-auto" aria-hidden="true" />}</label>; })}</div>; }

function DiscoveryResults({ recommendations, answers, packages, turnstileToken, setTurnstileToken, busy, error, onSend, onStartOver }: { recommendations: Array<ReturnType<typeof scoreDestinations>[number] & { destinationId?: string; displayName: string }>; answers: DiscoveryAnswers; packages: Package[]; turnstileToken: string; setTurnstileToken: (token: string) => void; busy: boolean; error: boolean; onSend: () => void; onStartOver: () => void }) {
  const t = useTranslations("discover");
  return <section aria-labelledby="discovery-results-title"><div className="mb-8 flex flex-wrap items-start justify-between gap-4"><div className="space-y-3"><p className="t-small text-brand">{t("resultEyebrow")}</p><h2 id="discovery-results-title" className="t-h2">{t("resultTitle")}</h2><p className="t-body-lg measure text-ink-soft">{t("resultIntro")}</p></div><Button type="button" variant="ghost" onClick={onStartOver}><RotateCcw size={18} strokeWidth={1.75} aria-hidden="true" />{t("changeAnswers")}</Button></div><div className="grid gap-6 lg:grid-cols-3">{recommendations.map(item => <article key={item.slug} className="flex h-full flex-col overflow-hidden rounded-card border border-line bg-canvas shadow-sm"><Link href={"/destinos/" + item.slug} className="group block"><div className="relative aspect-[4/3] overflow-hidden bg-surface"><Image src={discoveryImage(item.slug)} alt={t("destinationAlt", { name: item.displayName })} fill sizes="(max-width: 1023px) calc(100vw - 32px), (max-width: 1199px) calc((100vw - 72px) / 2), 368px" className="object-cover transition-transform duration-(--duration-reveal) ease-out group-hover:scale-[1.03] motion-reduce:transition-none" /></div><div className="p-6"><div className="flex items-start justify-between gap-4"><h3 className="t-h3">{item.displayName}</h3><ArrowRight size={20} strokeWidth={1.75} className="mt-1 shrink-0 text-brand" aria-hidden="true" /></div><p className="t-body mt-4 text-ink-soft">{item.why}</p><span className="t-small mt-4 inline-flex text-brand underline underline-offset-4">{t("viewDestination")}</span></div></Link><MatchingPackages destinationId={item.destinationId} destinationName={item.displayName} packages={packages} /></article>)}</div><div className="mt-8 rounded-panel border border-line bg-surface p-6 sm:p-8"><div className="grid gap-6 lg:grid-cols-[1fr_auto] lg:items-center"><div><h3 className="t-h3">{t("conversationTitle")}</h3><p className="t-body mt-2 text-ink-soft">{t("conversationBody")}</p></div><div className="min-w-0"><Turnstile onToken={setTurnstileToken} /><Button type="button" variant="whatsapp" disabled={busy || !turnstileToken} aria-busy={busy} onClick={onSend}><MessageCircle size={20} strokeWidth={1.75} aria-hidden="true" />{busy ? t("saving") : t("sendOptions")}</Button>{error && <p role="alert" className="t-small mt-3 text-error">{t("error")}</p>}</div></div></div><p className="t-small mt-6 text-ink-soft">{t("selected", { type: t("labels." + answers.travelerType).toLowerCase(), duration: t("labels." + answers.duration).toLowerCase(), count: answers.travelers })}</p></section>;
}

function MatchingPackages({ destinationId, destinationName, packages }: { destinationId?: string; destinationName: string; packages: Package[] }) { const t = useTranslations("discover"); const matches = packages.filter(item => (destinationId && item.destination_id === destinationId) || slugForDestination(item.destino) === slugForDestination(destinationName)); if (!matches.length) return null; return <div className="border-t border-line bg-surface p-6"><p className="t-small mb-4 text-ink-soft">{t("publishedPackages", { name: destinationName })}</p><div className="space-y-4">{matches.map(item => <div key={item.id} className="space-y-2"><Link href={"/paquetes/" + item.slug} className="t-body inline-flex min-h-12 items-center text-brand underline underline-offset-4">{item.nombre}</Link><p className="t-small text-ink-soft">{t("quote")}</p></div>)}</div></div>; }
