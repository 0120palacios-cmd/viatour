"use client";
import { useRef, useState } from "react";
import { useTranslations } from "next-intl";
import { Upload } from "lucide-react";
import { Button } from "@/components/ui/button";
export function DocumentUploadForm() { const t = useTranslations("portal"); const formRef = useRef<HTMLFormElement>(null); const [status, setStatus] = useState(""); const [busy, setBusy] = useState(false); async function submit(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); setBusy(true); setStatus(""); try { const response = await fetch("/api/mi-reserva/documentos", { method: "POST", body: new FormData(event.currentTarget) }); const body = await response.json().catch(() => ({})); if (!response.ok) throw new Error(typeof body.error === "string" ? body.error : t("genericError")); formRef.current?.reset(); setStatus(t("saved")); window.location.reload(); } catch (error) { setStatus(error instanceof Error ? error.message : t("genericError")); } finally { setBusy(false); } } return <form ref={formRef} onSubmit={submit} className="mt-6 space-y-4" aria-busy={busy}><label className="t-small block" htmlFor="portal-documento">{t("documentLabel")}</label><input id="portal-documento" name="documento" type="file" accept="application/pdf,image/jpeg,image/png,image/webp" required className="t-small block w-full rounded-btn border border-line bg-canvas p-3" /><Button type="submit" disabled={busy}><Upload size={18} strokeWidth={1.75} aria-hidden="true" />{busy ? t("uploading") : t("upload")}</Button>{status ? <p className="t-small text-ink-soft" role="status">{status}</p> : null}</form>; }
