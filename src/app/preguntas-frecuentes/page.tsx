import type { Metadata } from "next";
import { getTranslations } from "next-intl/server";
import { localizedPageMetadata } from "@/lib/seo";
import { getFAQs, type FAQ } from "@/lib/faqs";
import { Accordion, AccordionItem, AccordionTrigger, AccordionContent } from "@/components/ui/accordion";
export function generateMetadata(): Promise<Metadata> { return localizedPageMetadata("/preguntas-frecuentes", "faq"); }
export default async function Page() { const t = await getTranslations("static"); const common = await getTranslations("common"); let faqs: FAQ[] = []; let failed = false; try { faqs = await getFAQs(); } catch { failed = true; } const groups = new Map<string, FAQ[]>(); for (const f of faqs) { const category = f.categoria?.trim() || "General"; groups.set(category, [...(groups.get(category) || []), f]); } const schema = { "@context": "https://schema.org", "@type": "FAQPage", mainEntity: faqs.map(f => ({ "@type": "Question", name: f.pregunta, acceptedAnswer: { "@type": "Answer", text: f.respuesta } })) }; return <main className="container-site py-14 sm:py-24"><h1 className="t-h1 mb-8">{common("faq")}</h1><p className="t-small mb-8 text-ink-soft">{t("faqDraft")}</p>{failed ? <p role="alert">{t("faqLoadError")}</p> : !faqs.length ? <p className="rounded-panel border bg-surface p-8">{t("faqEmpty")}</p> : <div className="measure space-y-12">{Array.from(groups, ([category, items]) => <section key={category}><h2 className="t-h2 mb-4">{category}</h2><Accordion type="multiple">{items.map(f => <AccordionItem key={f.id} value={f.id}><AccordionTrigger>{f.pregunta}</AccordionTrigger><AccordionContent>{f.respuesta}</AccordionContent></AccordionItem>)}</Accordion></section>)}</div>}{faqs.length > 0 && <script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(schema).replace(/</g, "\\u003c") }} />}</main>; }
