/* eslint-disable @next/next/no-img-element -- ImageResponse renders PNG on the server; next/image cannot be used in Satori. */
import { ImageResponse } from "next/og";
import { readFile } from "node:fs/promises";
import { join } from "node:path";
export async function GET(request: Request) { const { searchParams } = new URL(request.url); const locale = searchParams.get("locale") === "en" ? "en" : "es"; const title = (searchParams.get("title")?.trim() || (locale === "en" ? "Travel advice from Honduras" : "Asesoría para planificar su viaje desde Honduras")).slice(0, 100); const subtitle = locale === "en" ? "Travel advisors from Honduras" : "Asesores de viaje desde Honduras"; const logo = await readFile(join(process.cwd(), "public/logo-black.png")); const logoHeight = Math.round(280 * logo.readUInt32BE(20) / logo.readUInt32BE(16)); return new ImageResponse(<div style={{ display: "flex", flexDirection: "column", width: "100%", height: "100%", background: "#FFFFFF", borderBottom: "24px solid #1656D6", padding: "64px 80px", justifyContent: "space-between" }}><div style={{ display: "flex", alignItems: "center" }}><img src={`data:image/png;base64,${logo.toString("base64")}`} width={280} height={logoHeight} alt="viatour" /></div><div style={{ display: "flex", flexDirection: "column", gap: "18px" }}><div style={{ display: "flex", fontSize: 46, fontWeight: 700, color: "#12161C", lineHeight: 1.15 }}>{title}</div><div style={{ display: "flex", fontSize: 28, color: "#5A6672" }}>{subtitle}</div></div></div>, { width: 1200, height: 630 }); }
